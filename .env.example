# Extended Exchange Trading Bot .env

# Go to https://app.extended.exchange/api-management and make a new API Key
# (DO NOT SHARE THIS WITH ANYONE, DO NOT COMMIT THIS TO GITHUB - .gitignore ensures this)

# IMPORTANT: Formatting Rules
# - After the = sign, no space, quotations, or parentheses around values
# - Example: EXT_API_KEY=your_key_here

# API Credentials (from Extended Exchange UI -> API Management)
EXT_API_KEY=                    # API Key
L2_PUBLIC_KEY=                  # Starknet Public Key
EXT_L2_KEY=                     # Starknet Private Key
EXT_L2_VAULT=                   # Vault ID
EXT_USER_AGENT=extended-bot/1.0  # User Agent
EXT_BASE_URL=https://api.starknet.extended.exchange/api/v1  # API Endpoint

# Trading Configuration
LONG_PAIR=BTC-USD               # Long position trading pair
SHORT_PAIR=ETH-USD              # Short position trading pair
LONG_LEVERAGE=10                # Leverage for long position
SHORT_LEVERAGE=10               # Leverage for short position
TARGET_SIZE=200.0               # Target position size in USD
OPEN_TIME=21:30:30              # Time to open positions (HH:MM:SS)
CLOSE_TIME=18:30:30             # Time to close positions (HH:MM:SS)
TIMEZONE=UTC                    # Timezone for trading schedule

# Optional Configuration
LOG_LEVEL=INFO                  # Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_FILE=/app/logs/trading-bot.log  # Log file path
DEAD_MAN_SWITCH_ENABLED=true    # Enable dead man's switch (cancels orders on startup)
MAX_RETRY_ATTEMPTS=3            # Maximum retry attempts for failed operations

# DigitalOcean Configuration (metadata only, doesn't affect bot functionality)
DO_REGION=syd1                  # DigitalOcean region
DO_DROPLET_SIZE=s-1vcpu-1gb     # DigitalOcean droplet size
